version: '3.8'

services:
  prover-api:
    build: 
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
      - NODE_ENV=development
    volumes:
      - ./circuit:/app/circuit
      - ./prover_api:/app/prover_api
